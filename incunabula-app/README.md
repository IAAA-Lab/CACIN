# Requirements and execution of the web frontend

## Node version
Install a compatible version of `Node.js`, as a prerequisite `Node.js 14.0.0`.

## Execution
Install the dependencies and run the code (in the project directory)

```bash
npm install
npm start
```
